apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: my-first-app
    component: backend
spec:
  type: ClusterIP
  selector:
    app: my-first-app
    component: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
